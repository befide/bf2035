
.layout {
  position relative;
}

.layout__main, .layout__header {
  position relative;
  margin 0 auto;
  transition 200ms;
}


[data-fit-spread=true] #layout__main__wrapper {
  transform-origin 0 0;
  transform: scale(1);
  
  }
 
.layout__main {
  
  margin: 1rem;
  
  // width: 100%;
  // overflow: hidden;
  // margin auto;
  // position: absolute
  aspect-ratio 1;

}