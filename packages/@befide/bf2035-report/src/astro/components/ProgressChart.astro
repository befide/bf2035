---
// import Svg from "@jasikpark/astro-svg-loader";

export interface Props {
  name: string;
  label: string;
}

// const files =  await Astro.glob("./../../../../bf2035-charts/.tmp/*.svg");

// const { name } = Astro.props as Props;
// const { default: innerHTML } = await import(`${FOLDER}${name}`);

// console.log(files.map(f => f.default));

// function getSVG(name: string) {
//   const filepath = `${FOLDER}${name}.svg`;
//   const files = import.meta.glob<string>('/src/svg/**/*.svg', {
//     as: 'raw',
//     eager: true
//   });

//   if (!(filepath in files)) {
//     throw new Error(`${filepath} not found`);
//   }

//   const root = parse(files[filepath]);

//   const svg = root.querySelector('svg');
//   const { attributes, innerHTML } = svg;

//   return {
//     attributes,
//     innerHTML,
//   };
// }

// const svgMap = Object.entries(import.meta.glob("./../../../../bf2035-charts/.tmp/*.svg", {

// }))

// // const svgMap2 = svgMap.map(([k, v]) => [k.replace("../../../../bf2035-charts/.tmp/", ""), v]);

// const entry = svgMap2["progress-chart.progress__srf_gradient.svg"]

// console.log(svgMap);
---

<!-- <Svg src={files.src} /> -->
