---
import BaseLayout from '@layouts/BaseLayout.astro';

export interface Props {
  lang?: string;
  title?: string;
}

const { lang = "de", title= "NO TITLE"} = Astro.props;
---

<BaseLayout {title} {lang}>
  <slot />
</BaseLayout>
