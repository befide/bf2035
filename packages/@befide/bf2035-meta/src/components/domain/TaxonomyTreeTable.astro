---
import TaxonomyTreeVue from './TaxonomyTree.vue';

const { acceleratorResearchSpecific } = Astro.props;
---

<TaxonomyTreeVue
  client:only="true"
  acceleratorResearchSpecific={acceleratorResearchSpecific}
/>
